# trafficflow-posix-simulator
A multithreaded C++ traffic-network simulator that models shared bridges, ferries, and intersections using POSIX threads, mutexes, condition variables, timed waits, and monitor-style synchronization.
